name: Build_App

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    -cron: "0 */12 * * *"

jobs:
  build:
    runs-on: [windows-latest]
    steps:
    - uses: actions/checkout@v2

    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v1.0.1
